---
import { getLangFromUrl, useTranslations } from '../i18n/index';
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const year = new Date().getFullYear();
---
<footer class="bg-slate-50 dark:bg-navy-950 border-t border-slate-200 dark:border-slate-700 mt-16">
  <div class="max-w-6xl mx-auto px-4 py-8 text-center text-slate-500 dark:text-slate-400 text-sm">
    <p>© {year} Sérgio Carvalho. {t('footer.rights')}.</p>
  </div>
</footer>
